<%= form_for(@doi_url) do |f| %>
  <% if @doi_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doi_url.errors.count, "error") %> prohibited this doi_url from being saved:</h2>

      <ul>
      <% @doi_url.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :doi_id %><br>
    <%= f.text_field :doi_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
